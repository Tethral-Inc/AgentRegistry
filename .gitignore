node_modules/
dist/
.env
.env.*
!.env.example
*.tfstate
*.tfstate.backup
.terraform/
.vercel
.wrangler
__pycache__/
*.egg-info/
coverage/
.turbo
.env*.local

# Build artifacts and bundles (may contain embedded credentials)
scripts/*-bundle.*
scripts/run-crawler-local.*
scripts/backfill-scan-bundle.*

# Next.js build output
.next/

# TypeScript incremental build cache — ends up tracked accidentally
# because tsc writes it next to tsconfig.json, not into dist/.
*.tsbuildinfo

# IDE / OS
.DS_Store
*.swp
.claude/settings.local.json
.mcp.json

.env*.local

# MCP Registry publisher
.mcpregistry_*
mcp-publisher.exe
